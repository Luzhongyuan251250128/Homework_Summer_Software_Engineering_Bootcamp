# REFLECTION.md — 研发任务智能统计与工时分析平台

## 一、项目与我

这个项目的起点是一句话："团队导入 Git 数据，自动统计开发工时与提交频率，用大模型单轮生成周报和迭代风险分析。"整门课我最深的体会是：当 AI 能写大部分代码时，我的价值不在敲键盘，而在想清楚"做什么"和"做对了吗"。整个开发周期里，我同时扮演 PM、架构师和 reviewer——定方向的是我，被质询的是我，最后签字放行的也是我。

## 二、brainstorming：追问逼我修正原设想

我最初的设想是"自动统计开发工时"，但 brainstorming 第一个问题就把我按住了：**Git 数据里根本没有"工时"字段**。系统拿什么把 commit 换算成工时？这个追问让我从"自动统计"退回到"估算口径 + 人工校正"：按提交时间聚类活跃段、按代码量加权，口径公开可配置、允许管理者覆盖。没有这一问，SPEC 会写成一份无法落地的空想文档。

另两个修正同样来自追问：报告从"生成"变成"生成 → 编辑 → 存历史 → 导出"的完整闭环（AI 草稿必须人来定稿）；范围上用 YAGNI 砍掉了多团队 SaaS、多用户账号这些听着很酷但根本做不完的东西。**好的 brainstorming 不是帮我确认想法，而是帮我把不成熟的想法拆掉重来。**

## 三、冷启动验证：规划文档最诚实的审阅者

课程要求正式实现前，换一个完全陌生的 agent，只给 SPEC 和 PLAN 试跑。我用 opencode 开了全新会话去实现 Task 4/5，结果它**六次暂停提问，抓出四类缺陷**：三个测试的期望值算错（段封顶测试的提交间隔会先被聚类拆开、代码量系数忘了 clamp 上限）、T3 断言一个 T9 才存在的路由、以及"人工校正覆盖"这个 SPEC 验收项根本没有 task 落地。

更值得记录的是，实现阶段 20 个 task 的 subagent 们又陆续抓出 7 处同类问题——合计 11 处缺陷**全部集中在测试期望值和依赖标注层，SPEC 的口径与架构零错误**。最典型的是 T11：计划标注"可与 T10 并行"，实际它的文件顶部 `from .stats import iteration_stats` 依赖 T10 的产物，导致一次调度阻塞。这让我彻底相信：**一份能被零上下文 agent 顺畅执行的规划文档，才是真正写清楚了的文档；而单人项目里，冷启动验证是最接近同侪评审的机制。**

## 四、subagent 驱动与 TDD：AI 协作下的纪律

实现阶段每个 task 开独立 worktree 和分支，派一个全新 subagent，两阶段评审（先 spec 合规、再代码质量）通过才合入。这套流程让我看到 subagent 的真正价值不是"更会写代码"，而是**用它的"无知"给文档挑错**——它不知道我们的共享上下文，只能在文档字面处受阻，受阻点就是文档缺陷。

TDD 在这种协作下不是阻碍，而是放大器：**失败测试就是给 subagent 的验收定义**，红→绿目标明确，它既不会跑偏也不会过度实现。二十个 task 里，凡是测试写得精确的，subagent 都一次到位；凡是测试期望值算错的（如 2000 行代码量系数应得 1.5 小时我写成 2.0），全靠冷启动和后续 agent 纠回来。约 2–5 分钟一个步骤的颗粒度刚好——大到有独立验收点，小到一次会话能完成。

## 五、凭据与分发：最不像代码的工程课

凭据与分发这两条要求，逼我想清楚了平时根本不会碰的问题：Git Token 用主密钥派生的 AES-256-GCM 加密后入库、页面只显示末四位；主密钥走系统钥匙串；LLM Key 环境变量优先、也可网页录入加密存储；`.env` 绝不进仓库；提交前全仓库扫描凭据。分发选了 Docker 多阶段构建，并让容器监听 `$PORT` 以适配云平台。没有这门课，这些"看不见的工程"我大概率会全部跳过——而它们恰恰是系统能否被陌生人安全跑起来的分水岭。

## 六、批判与重做

对 Superpowers 这套方法论，我有三点批评。第一，它假设 agent 会严格按文档字面执行，但文档的测试层一旦出错，agent 会诚实卡住——所以规划阶段就应做冷启动验证，而不是等到实现。第二，它假设工具环境一致，我这边前端测试因沙箱限制跑不了，只能降级为静态类型检查加人工验证，这类环境问题应该在计划里提前探测而不是让每个 task 各自踩坑。第三，也是最扎心的：**流程守住了 TDD 和评审，却阻止不了 AI 编造事实**——我早期在 AGENT_LOG 里编造过时间戳、在 SPEC 里虚假声称用了 Open Design，都是被你当场质询才纠正的。这说明纪律必须由人来执行，"流程脚手架"替代不了人的判断。

如果重做，我会：第一天就把冷启动验证做掉；并行派发前逐行核对跨 task 的 import 依赖；所有测试期望值手算一遍再写进 PLAN；前端测试环境提前探测并统一降级方案。

（AI 辅助润色声明：本报告的事实与观点均来自本人开发过程的真实记录；文字由 AI 辅助起草整理，本人已通读并按自己的经历修改后定稿。）
